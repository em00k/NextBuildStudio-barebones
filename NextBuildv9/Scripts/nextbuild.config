# NextBuild Configuration File
# Paths to major components and tools

# Core directories
CSPECT=Emu/CSpect
ZXBASIC=zxbasic1.18.1
TOOLS=Tools

# File paths 
IMG_FILE=img/cspect-next-2gb.img

# External tools
HDFMONKEY=Tools/hdfmonkey

# CSpect settings
CSPECT_ARGS=-w3 -16bit -brk -tv -vsync -nextrom -basickeys

# Additional settings (optional)
DEFAULT_HEAP=1024
DEFAULT_ORG=32768
DEFAULT_OPTIMIZE=4

# NextZXOS settings
NEXTZXOS_ENABLED=false
NEXTZXOS_PATH=
